---
interface Props {
  title?: string;
  tone?: "note" | "tip" | "warning";
}

const { title = "Note", tone = "note" } = Astro.props;
---

<aside class:list={["callout", `callout-${tone}`]}>
  <strong>{title}</strong>
  <div><slot /></div>
</aside>
